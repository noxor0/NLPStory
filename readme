Makes a story with the given text documents.

3D Hashmap used to store the trigrams

Creates a weighted array with of the possible next words, and chooses the
next word.

Pass in all stories as arguments. (Python 2.7)
ls *.txt | xargs python story.py
